<UserControl x:Class="Small_square_cavity_coating_machine.Views.HistoryModules.AlarmHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="580"
             d:DesignWidth="1000">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,10" Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Margin="0,0,6,0" VerticalAlignment="Center" Text="开始时间"/>
            <DatePicker Style="{StaticResource HistoryDatePickerStyle}"
                        SelectedDate="{Binding StartDate}"/>
            <TextBox Width="78" Margin="5,0,15,0" VerticalContentAlignment="Center"
                     Text="{Binding StartTimeText, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Margin="0,0,6,0" VerticalAlignment="Center" Text="结束时间"/>
            <DatePicker Style="{StaticResource HistoryDatePickerStyle}"
                        SelectedDate="{Binding EndDate}"/>
            <TextBox Width="78" Margin="5,0,15,0" VerticalContentAlignment="Center"
                     Text="{Binding EndTimeText, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Width="82" Height="32" Margin="0,0,8,0"
                    Content="查询" Command="{Binding QueryCoreCommand}"/>
            <Button Width="90" Height="32"
                    Content="重置时间" Command="{Binding ClearFiltersCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  ItemsSource="{Binding Records}"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  EnableRowVirtualization="True"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Width="170" Header="报警时间"
                                    Binding="{Binding OccurredAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}"/>
                <DataGridTextColumn Width="90" Header="等级" Binding="{Binding Severity}"/>
                <DataGridTextColumn Width="145" Header="设备/来源" Binding="{Binding Source}"/>
                <DataGridTextColumn Width="*" MinWidth="240" Header="报警内容" Binding="{Binding Message}"/>
                <DataGridTextColumn Width="170" Header="清除时间"
                                    Binding="{Binding ClearedAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}"/>
                <DataGridTextColumn Width="90" Header="状态" Binding="{Binding StatusText}"/>
                <DataGridTextColumn Width="70" Header="来源" Binding="{Binding DataSourceText}"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Margin="2,8,0,0"
                   Foreground="#607D8B" Text="{Binding StatusMessage}"/>
    </Grid>
</UserControl>
