<UserControl x:Class="Small_square_cavity_coating_machine.Views.HistoryModules.ProcessHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Small_square_cavity_coating_machine.Views.HistoryModules">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="110"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="*" MinHeight="230"/>
        </Grid.RowDefinitions>
        <WrapPanel Margin="0,0,0,8">
            <TextBlock Text="开始日期" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <DatePicker SelectedDate="{Binding StartDate}" Style="{StaticResource HistoryDatePickerStyle}"/>
            <TextBlock Text="结束日期" VerticalAlignment="Center" Margin="16,0,8,0"/>
            <DatePicker SelectedDate="{Binding EndDate}" Style="{StaticResource HistoryDatePickerStyle}"/>
            <Button Content="查询" Command="{Binding QueryCommand}" MinWidth="80" Padding="12,6" Margin="12,0,0,0"/>
            <Button Content="导出当前任务" Command="{Binding ExportCommand}" Padding="12,6" Margin="8,0,0,0"/>
        </WrapPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Runs}" SelectedItem="{Binding SelectedRun}"
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False"
                  SelectionMode="Single" SelectionUnit="FullRow" HeadersVisibility="Column"
                  EnableRowVirtualization="True" MinHeight="70">
            <DataGrid.Columns>
                <DataGridTextColumn Header="配方名" Binding="{Binding RecipeName}" Width="*" MinWidth="120"/>
                <DataGridTextColumn Header="开始时间" Binding="{Binding StartedAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="180"/>
                <DataGridTextColumn Header="结束时间" Binding="{Binding EndedAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="180"/>
                <DataGridTextColumn Header="执行状态" Binding="{Binding Status}" Width="110"/>
                <DataGridTextColumn Header="记录状态" Binding="{Binding RecordingStatus}" Width="2*" MinWidth="180"/>
            </DataGrid.Columns>
        </DataGrid>
        <GridSplitter Grid.Row="2" Height="6" HorizontalAlignment="Stretch" Background="#D3DDE5"/>
        <local:TrendChartView Grid.Row="3"/>
    </Grid>
</UserControl>
